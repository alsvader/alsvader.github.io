const styles = (theme) => ({
  root: {
    flexGrow: 1,
  },
  menuButton: {
    marginRight: theme.spacing(0),
  },
  title: {
    flexGrow: 1,
    fontWeight: 'bold',
  },
  buttonOption: {
    marginRight: theme.spacing(2),
  },
});

export default styles;
